*.beam
*.ez
**/build
erl_crash.dump

**/node_modules/
*.sqlite*
*.db*

knowledge/repos/

STATUS.md

# Lustre dev binaries
**/.lustre/bin/

# ── Parrot (sqlc codegen) ─────────────────────────────────────
# The build/.parrot/ directory contains the auto-downloaded sqlc binary
# and intermediate files. Regenerate with: mise run yard:gen
# The generated sql.gleam IS checked in (see note below).
**/build/.parrot/
